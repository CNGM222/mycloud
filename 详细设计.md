# 个人网盘系统详细设计

## 1. 文档说明

本文档是个人网盘系统详细设计的主索引文档，用于衔接《需求文档.md》和《概要设计.md》，指导后续编码、测试和交付。

系统固定采用以下技术路线：

- 后端：Java + Spring Boot + Spring MVC。
- 数据访问：JDBC + HikariCP。
- 数据库：MySQL 9.6。
- 网页端：Vue 3 + TypeScript + Vite。
- Windows 客户端：Electron + Vue 3。
- Android 客户端：Kotlin 原生 Android。
- 认证方式：JWT Bearer Token。
- 文件内容存储：后端服务器本地文件系统，通过 Java 流式读写。
- 文件元数据存储：MySQL 9.6，保存目录关系、文件属性、存储键、容量、任务和日志。

## 2. 文档拆分

详细设计按子系统拆分为以下文件：

| 文档 | 内容 |
| --- | --- |
| 详细设计-数据库设计.md | 数据表、字段、索引、建表 SQL、本地存储和一致性设计 |
| 详细设计-后端设计.md | Spring Boot 包结构、类职责、核心流程、权限和异常 |
| 详细设计-接口设计.md | REST API 请求头、请求参数、响应字段、错误码 |
| 详细设计-Vue3网页端设计.md | 网页端路由、页面、组件、状态管理、API 封装 |
| 详细设计-Windows客户端设计.md | Electron 客户端窗口、主进程、渲染进程、本地能力 |
| 详细设计-Android客户端设计.md | Kotlin Android 页面、网络层、存储、权限、任务 |
| 详细设计-流程图与类图.md | 系统总体、后端模块、客户端模块的流程图和类图 |
| 详细设计-测试设计.md | 单元、接口、功能、安全、一致性和性能测试 |

建议阅读顺序：

1. 详细设计.md。
2. 详细设计-数据库设计.md。
3. 详细设计-接口设计.md。
4. 详细设计-后端设计.md。
5. 详细设计-Vue3网页端设计.md。
6. 详细设计-Windows客户端设计.md。
7. 详细设计-Android客户端设计.md。
8. 详细设计-流程图与类图.md。
9. 详细设计-测试设计.md。

## 3. 系统边界

系统由一个统一后端和三个客户端组成。

```text
Vue 3 网页端
Windows Electron 客户端
Android Kotlin 客户端
        |
        | REST API / HTTPS / JWT
        v
Java Spring Boot 后端
        |-- JDBC / HikariCP --> MySQL 9.6（文件元数据和业务数据）
        |
        +-- Java NIO --> 服务器本地文件系统（文件二进制内容）
```

三端只通过 HTTP API 访问后端，不直接连接数据库或服务器文件系统。所有身份认证、权限校验、容量计算、文件内容读写和日志记录均由后端完成。

## 4. 模块边界

| 模块 | 主要职责 |
| --- | --- |
| 用户认证模块 | 注册、登录、退出、JWT 校验、用户信息 |
| 文件管理模块 | 文件列表、上传、下载、新建文件夹、重命名、移动、删除 |
| 搜索模块 | 按文件名、类型、时间范围搜索当前用户文件 |
| 容量模块 | 总容量、已用容量、上传前校验、删除后释放 |
| 任务模块 | 上传下载任务记录、状态查询、失败重试 |
| 管理员模块 | 用户查询、用户状态、容量调整、系统统计 |
| 日志模块 | 用户和管理员关键操作日志 |

## 5. 统一约定

### 5.1 客户端类型

`X-Client-Type` 取值固定为：

- `web`
- `windows`
- `android`

### 5.2 角色

`role` 取值固定为：

- `user`
- `admin`

### 5.3 用户状态

`status` 取值固定为：

- `normal`
- `disabled`

### 5.4 文件类型

`files.type` 取值固定为：

- `file`
- `folder`

### 5.5 任务状态

`tasks.status` 取值固定为：

- `pending`
- `running`
- `success`
- `failed`
- `cancelled`

## 6. 核心数据流

### 6.1 登录数据流

```text
客户端提交账号密码
 -> 后端校验用户和密码
 -> 后端生成 JWT
 -> 客户端保存 JWT
 -> 后续请求携带 Authorization 头
```

### 6.2 文件上传数据流

```text
客户端选择文件
 -> multipart/form-data 上传
 -> 后端校验 JWT 和目录权限
 -> 后端校验容量和重名策略
 -> 流式写入本地临时文件并计算大小和哈希
 -> 原子移动到正式存储目录
 -> 数据库写入文件元数据和相对存储键
 -> 更新用户容量
 -> 记录任务和日志
 -> 返回文件信息
```

### 6.3 文件下载数据流

```text
客户端请求文件下载
 -> 后端校验 JWT 和文件归属
 -> 查询文件元数据
 -> 根据相对存储键流式读取本地文件
 -> HTTP 响应流输出
 -> 记录任务和日志
```

### 6.4 三端一致性

三端不维护独立远程文件状态。上传、删除、移动、重命名成功后，客户端应重新请求文件列表或局部刷新当前目录，确保展示与后端一致。

## 7. 开发约束

- 后端不得使用 ORM 框架替代 JDBC。
- 前端和客户端不得保存数据库连接信息。
- 客户端不得保存用户明文密码。
- 文件内容必须写入后端配置的本地存储根目录，数据库只保存元数据和相对存储键。
- 客户端、接口响应和业务日志不得暴露服务器真实存储路径。
- 存储键必须由后端生成，不能直接使用用户文件名拼接物理路径。
- 文件相关操作必须以 `user_id` 作为权限边界。
- 元数据、容量、任务和日志变更必须使用数据库事务；文件系统操作使用临时目录、原子移动、失败补偿和定期巡检保证最终一致性。

## 8. 交付物

最终应交付：

- 数据库初始化 SQL。
- Spring Boot 后端代码。
- Vue 3 网页端代码。
- Windows Electron 客户端代码。
- Android Kotlin 客户端代码。
- 测试用例和测试报告。
- 部署说明。
