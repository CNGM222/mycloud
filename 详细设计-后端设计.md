# 详细设计-后端设计

## 1. 后端技术栈

- Java。
- Spring Boot。
- Spring MVC。
- JDBC。
- HikariCP。
- MySQL 9.6 JDBC 驱动。
- Java NIO 本地文件系统 API。
- JWT Bearer Token。

## 2. 包结构设计

```text
com.mycloud
  MyCloudApplication
  config
    WebConfig
    DataSourceConfig
    SecurityConfig
  controller
    AuthController
    UserController
    FileController
    FolderController
    SearchController
    TaskController
    AdminController
  service
    AuthService
    UserService
    FileService
    SearchService
    TaskService
    AdminService
    OperationLogService
  storage
    FileStorage
    LocalFileStorage
    StorageKeyGenerator
    StorageReconciliationJob
  dao
    UserDao
    FileDao
    TaskDao
    OperationLogDao
  model
    entity
    dto
    vo
    enums
  security
    JwtTokenProvider
    JwtAuthenticationFilter
    CurrentUser
  exception
    BusinessException
    ErrorCode
    GlobalExceptionHandler
  util
    Md5Utils
    FileNameUtils
    StreamUtils
    PageUtils
```

## 3. 通用设计

### 3.1 统一响应

```java
public class ApiResponse<T> {
    private int code;
    private String message;
    private T data;
}
```

成功：

```json
{ "code": 0, "message": "success", "data": {} }
```

失败：

```json
{ "code": 40001, "message": "未登录或登录已过期", "data": null }
```

### 3.2 当前用户

认证过滤器解析 JWT 后生成 `CurrentUser`：

```java
public class CurrentUser {
    private Long userId;
    private String username;
    private String role;
    private String clientType;
}
```

Controller 不信任请求参数中的用户 ID，所有当前用户身份均从 JWT 解析结果获取。

### 3.3 错误码

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40000 | 参数错误 |
| 40001 | 未登录或登录已过期 |
| 40003 | 权限不足 |
| 40004 | 资源不存在 |
| 40009 | 文件名冲突 |
| 40010 | 容量不足 |
| 40011 | 文件大小超过限制 |
| 50000 | 系统内部错误 |
| 50010 | 文件存储不可用或内容不一致 |

## 4. 认证设计

### 4.1 JWT 内容

JWT payload 包含：

| 字段 | 说明 |
| --- | --- |
| sub | 用户 ID |
| username | 用户名 |
| role | user 或 admin |
| iat | 签发时间 |
| exp | 过期时间 |

默认过期时间：7 天。

### 4.2 AuthService

核心方法：

```java
RegisterResult register(RegisterRequest request);
LoginResult login(LoginRequest request, String clientType, String ip);
void logout(CurrentUser currentUser, String clientType, String ip);
CurrentUser parseToken(String token);
```

处理规则：

- 注册时校验用户名、邮箱、密码。
- 密码使用 BCrypt 哈希。
- 登录时支持用户名或邮箱。
- 账号状态为 `disabled` 时禁止登录。
- 退出登录由客户端删除 token，后端记录日志。

## 5. 用户模块

### 5.1 UserController

接口：

```text
GET /api/user/profile
```

### 5.2 UserService

核心方法：

```java
UserProfileVO getProfile(CurrentUser currentUser);
```

返回字段：

- id。
- username。
- email。
- role。
- totalCapacity。
- usedCapacity。
- status。
- createdAt。

## 6. 文件模块

### 6.1 FileController

接口：

```text
GET /api/files
POST /api/files/upload
GET /api/files/{id}/download
PATCH /api/files/{id}/rename
PATCH /api/files/{id}/move
DELETE /api/files/{id}
```

### 6.2 FolderController

接口：

```text
POST /api/folders
```

### 6.3 FileService

核心方法：

```java
PageResult<FileItemVO> listFiles(CurrentUser user, Long parentId, int page, int size);
FileItemVO createFolder(CurrentUser user, CreateFolderRequest request);
FileItemVO upload(CurrentUser user, UploadContext context);
void download(CurrentUser user, Long fileId, HttpServletResponse response);
FileItemVO rename(CurrentUser user, Long fileId, RenameRequest request);
FileItemVO move(CurrentUser user, Long fileId, MoveRequest request);
void delete(CurrentUser user, Long fileId);
```

### 6.4 上传实现

上传步骤：

1. 校验 `parentId`，为空表示根目录。
2. 校验目标目录属于当前用户且类型为 `folder`。
3. 校验文件名合法。
4. 按重名策略处理，默认自动追加 `(1)`、`(2)`。
5. 获取文件大小并校验容量。
6. 创建 `running` 状态任务。
7. 生成随机存储键，将上传流写入本地临时文件，同时计算实际大小和 MD5。
8. 校验临时文件和磁盘剩余空间，将临时文件原子移动到正式存储目录。
9. 开启数据库事务并锁定用户容量记录，再次校验容量。
10. 插入 `files`，保存相对 `storage_key`，状态设为 `active`。
11. 更新 `users.used_capacity`。
12. 更新任务状态为 `success`。
13. 写入操作日志并提交事务。

失败时：

- 回滚数据库事务。
- 删除临时文件或已经移动的正式文件；清理失败时记录告警并交给巡检任务。
- 任务状态改为 `failed`。
- 返回明确错误信息。

### 6.5 下载实现

下载步骤：

1. 校验文件存在且属于当前用户。
2. 校验目标类型为 `file`。
3. 创建下载任务。
4. 设置响应头 `Content-Type`、`Content-Disposition`、`Content-Length`。
5. 校验 `storage_status='active'`，使用 `storage_key` 在存储根目录内解析物理文件。
6. 拒绝路径越界、符号链接越界、文件缺失和大小不一致等异常。
7. 使用 `Files.newInputStream` 打开本地文件。
8. 分块写入 `ServletOutputStream`。
9. 更新任务状态。
10. 写入操作日志。

### 6.6 删除实现

删除步骤：

1. 查询目标节点。
2. 校验归属。
3. 如果是文件夹，递归查询子节点。
4. 统计文件总大小。
5. 在短事务中将相关文件的 `storage_status` 标记为 `deleting`，阻止新的下载。
6. 将相关物理文件原子移动到本次操作的隔离回收目录。
7. 在数据库事务中删除文件元数据、更新用户容量并写入日志。
8. 提交后异步清理隔离文件；失败时恢复物理文件并将状态改回 `active`。
9. 清理或恢复失败时将状态标记为 `error` 并告警，交由一致性巡检任务处理。

### 6.7 移动实现

移动步骤：

1. 查询源节点。
2. 查询目标目录。
3. 校验二者均属于当前用户。
4. 校验目标目录类型为 `folder`。
5. 若源节点为文件夹，检查目标目录不是源节点自身或其子目录。
6. 校验目标目录下名称不重复。
7. 更新 `parent_id`。

## 7. 搜索模块

### 7.1 SearchService

核心方法：

```java
PageResult<FileItemVO> search(CurrentUser user, SearchRequest request);
```

查询条件：

- `keyword`：文件名模糊匹配。
- `type`：`file` 或 `folder`。
- `startTime`。
- `endTime`。
- `page`。
- `size`。

SQL 必须包含 `user_id = ?`。

## 8. 任务模块

### 8.1 TaskService

核心方法：

```java
PageResult<TaskVO> listTasks(CurrentUser user, String status, int page, int size);
TaskVO getTask(CurrentUser user, Long taskId);
void cancel(CurrentUser user, Long taskId);
void retry(CurrentUser user, Long taskId);
```

规则：

- 用户只能查看自己的任务。
- `success` 任务不能取消。
- `failed` 任务可重试，重试行为由客户端重新发起上传或下载请求。

## 9. 管理员模块

### 9.1 AdminService

核心方法：

```java
PageResult<UserAdminVO> listUsers(AdminQueryRequest request);
UserAdminVO getUser(Long userId);
void updateUserStatus(Long userId, String status, CurrentUser admin);
void updateUserCapacity(Long userId, Long totalCapacity, CurrentUser admin);
StorageStatVO getStorageStat();
PageResult<OperationLogVO> listLogs(LogQueryRequest request);
```

规则：

- 所有管理员接口必须校验 `role=admin`。
- 调整容量时，新的总容量不能小于用户已用容量。
- 禁用用户后，该用户不能登录；已登录客户端下一次请求应被拒绝。
- 存储统计同时返回数据库容量汇总、本地存储卷总空间、可用空间和异常记录数量，不返回存储根目录路径。

## 10. 日志模块

### 10.1 OperationLogService

核心方法：

```java
void log(Long userId, String action, Long targetId, String targetType, String clientType, String ip);
```

关键操作：

- register。
- login。
- logout。
- upload。
- download。
- create_folder。
- rename。
- move。
- delete。
- admin_update_status。
- admin_update_capacity。

## 11. 本地文件存储设计

### 11.1 FileStorage

本地文件读写通过存储抽象集中处理，Controller 和 DAO 不直接拼接物理路径：

```java
public interface FileStorage {
    StoredObject writeToTemp(InputStream inputStream, long maxSize);
    void promote(StoredObject object);
    InputStream open(String storageKey);
    TrashEntry moveToTrash(String operationId, String storageKey);
    void restore(TrashEntry entry);
    void purge(String operationId);
}
```

实现要求：

- `LocalFileStorage` 只接受后端生成的相对存储键。
- 所有解析后的路径必须规范化并验证仍位于存储根目录内。
- 禁止跟随可导致越界的符号链接。
- 正式对象使用 UUID 等不可预测名称并按前缀分层，用户文件名只保存在数据库中。
- 临时写入、正式存储和隔离回收位于同一文件系统。
- 文件流由调用方使用 try-with-resources 关闭。

### 11.2 一致性巡检

`StorageReconciliationJob` 定时处理超时临时文件、孤立正式文件、过期隔离文件、数据库缺失文件和 `error` 状态记录。巡检操作必须记录结果和告警，不得静默删除无法确认归属的数据。

## 12. DAO 设计

DAO 只负责 SQL 执行和结果映射，不写业务规则。

示例方法：

```java
Optional<User> findByUsernameOrEmail(String account);
Optional<User> findById(Long id);
long insertFile(FileEntity file);
void updateStorageStatus(long fileId, String status);
List<FileEntity> findChildrenRecursive(long userId, long rootId);
```

所有 SQL 使用参数绑定，禁止字符串拼接用户输入。

## 13. 异常处理

`BusinessException` 包含：

- `ErrorCode code`。
- `String message`。

`GlobalExceptionHandler` 负责：

- 参数异常转换。
- 业务异常转换。
- 未认证异常转换。
- 系统异常记录日志并返回统一错误。

## 14. 配置项

建议配置：

```yaml
mycloud:
  jwt:
    secret: ${MYCLOUD_JWT_SECRET}
    expire-seconds: 604800
  upload:
    max-file-size: 2147483648
    buffer-size: 8192
  storage:
    root: ${MYCLOUD_STORAGE_ROOT}
    objects-dir: objects
    temp-dir: .tmp
    trash-dir: .trash
    min-free-space: 5368709120
    reconciliation-cron: "0 0 * * * *"
  default-capacity: 10737418240
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mycloud
    username: mycloud_app
    password: ${MYCLOUD_DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
```
