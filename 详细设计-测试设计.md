# 详细设计-测试设计

## 1. 测试目标

验证个人网盘系统满足需求文档和详细设计要求，重点覆盖：

- 用户认证。
- 文件管理。
- 容量管理。
- 三端一致性。
- 权限隔离。
- JDBC 流式文件读写。
- 管理员功能。

## 2. 单元测试

| 编号 | 模块 | 测试点 | 预期 |
| --- | --- | --- | --- |
| UT-001 | 用户 | 用户名为空 | 返回参数错误 |
| UT-002 | 用户 | 邮箱格式错误 | 返回参数错误 |
| UT-003 | 用户 | 密码哈希 | 数据库不保存明文 |
| UT-004 | 文件 | 文件名非法字符 | 返回参数错误 |
| UT-005 | 文件 | 同目录重名 | 自动重命名或返回冲突 |
| UT-006 | 文件 | 移动到自身 | 返回参数错误 |
| UT-007 | 文件 | 移动到子目录 | 返回参数错误 |
| UT-008 | 容量 | 上传前容量不足 | 返回容量不足 |
| UT-009 | 容量 | 删除后释放容量 | used_capacity 正确减少 |
| UT-010 | 权限 | 非本人文件 | 返回权限不足 |

## 3. 接口测试

### 3.1 用户接口

| 编号 | 接口 | 场景 | 预期 |
| --- | --- | --- | --- |
| API-001 | POST /api/auth/register | 正常注册 | 返回 userId |
| API-002 | POST /api/auth/register | 用户名重复 | 返回错误 |
| API-003 | POST /api/auth/login | 正确账号密码 | 返回 JWT |
| API-004 | POST /api/auth/login | 错误密码 | 返回登录失败 |
| API-005 | GET /api/user/profile | 携带有效 token | 返回用户信息 |
| API-006 | GET /api/user/profile | 不带 token | 返回未登录 |

### 3.2 文件接口

| 编号 | 接口 | 场景 | 预期 |
| --- | --- | --- | --- |
| API-101 | GET /api/files | 根目录 | 返回分页文件列表 |
| API-102 | POST /api/folders | 新建文件夹 | 返回文件夹对象 |
| API-103 | POST /api/files/upload | 上传文件 | 返回文件对象和任务 ID |
| API-104 | GET /api/files/{id}/download | 下载本人文件 | 返回文件流 |
| API-105 | PATCH /api/files/{id}/rename | 重命名 | 返回更新后对象 |
| API-106 | PATCH /api/files/{id}/move | 移动 | 目录关系更新 |
| API-107 | DELETE /api/files/{id} | 删除文件 | 文件不可再访问 |
| API-108 | DELETE /api/files/{id} | 删除文件夹 | 子节点一并删除 |

### 3.3 管理员接口

| 编号 | 接口 | 场景 | 预期 |
| --- | --- | --- | --- |
| API-201 | GET /api/admin/users | 管理员访问 | 返回用户列表 |
| API-202 | GET /api/admin/users | 普通用户访问 | 返回权限不足 |
| API-203 | PATCH /api/admin/users/{id}/status | 禁用用户 | 用户不能继续登录 |
| API-204 | PATCH /api/admin/users/{id}/capacity | 调整容量 | 容量更新 |
| API-205 | GET /api/admin/logs | 查询日志 | 返回分页日志 |

## 4. 功能测试

| 编号 | 功能 | 步骤 | 预期 |
| --- | --- | --- | --- |
| FT-001 | 注册登录 | 注册用户并登录 | 登录成功进入文件页 |
| FT-002 | 上传文件 | 上传普通文件 | 文件列表出现该文件 |
| FT-003 | 下载文件 | 下载已上传文件 | 文件内容一致 |
| FT-004 | 新建文件夹 | 根目录新建文件夹 | 列表出现文件夹 |
| FT-005 | 进入文件夹 | 点击文件夹 | 显示子目录内容 |
| FT-006 | 重命名 | 修改文件名 | 三端刷新后名称一致 |
| FT-007 | 移动 | 移动到其他目录 | 源目录消失，目标目录出现 |
| FT-008 | 删除 | 删除文件 | 文件不可见，容量减少 |
| FT-009 | 搜索 | 输入关键词 | 返回匹配文件 |
| FT-010 | 任务 | 上传或下载 | 任务进度和状态正确 |

## 5. 三端一致性测试

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| CONS-001 | 网页端上传文件 | Windows 和 Android 刷新后可见 |
| CONS-002 | Windows 上传文件 | 网页端和 Android 刷新后可见 |
| CONS-003 | Android 上传文件 | 网页端和 Windows 刷新后可见 |
| CONS-004 | 任一端重命名 | 其他端刷新后显示新名称 |
| CONS-005 | 任一端移动文件 | 其他端刷新后目录关系一致 |
| CONS-006 | 任一端删除文件 | 其他端刷新后不可见 |
| CONS-007 | 任一端上传或删除 | 其他端刷新后容量一致 |

## 6. 安全测试

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| SEC-001 | 未登录访问文件列表 | 返回未登录 |
| SEC-002 | 伪造文件 ID 下载他人文件 | 返回权限不足或资源不存在 |
| SEC-003 | 普通用户访问管理员接口 | 返回权限不足 |
| SEC-004 | token 过期后访问接口 | 返回未登录 |
| SEC-005 | SQL 注入字符作为搜索词 | 不影响数据库结构 |
| SEC-006 | 客户端本地检查 | 不保存明文密码 |

## 7. 性能测试

| 编号 | 场景 | 指标 |
| --- | --- | --- |
| PERF-001 | 1GB 文件上传 | 后端内存不随文件大小线性增长 |
| PERF-002 | 1GB 文件下载 | 文件内容完整，服务稳定 |
| PERF-003 | 单目录 10000 条记录分页 | 分页响应可接受 |
| PERF-004 | 20 个并发上传 | 服务不崩溃，任务状态正确 |
| PERF-005 | 关键词搜索 | 能分页返回结果 |

## 8. 数据库测试

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| DB-001 | 上传失败中断 | 无孤立文件元数据 |
| DB-002 | 文件内容写入失败 | 事务回滚 |
| DB-003 | 删除文件夹 | 子文件内容同时删除 |
| DB-004 | 容量约束 | used_capacity 不为负 |
| DB-005 | JDBC 流式读取 | 不一次性加载完整 BLOB |

## 9. 验收标准

系统通过验收需要满足：

- 所有用户、文件、搜索、任务、管理员核心接口通过测试。
- 三端使用同一账号能看到一致的文件数据。
- 文件上传后可完整下载，MD5 一致。
- 用户不能访问其他用户文件。
- 上传和删除后容量统计正确。
- 管理员能管理用户状态和容量。
- 大文件上传下载时后端保持稳定。
