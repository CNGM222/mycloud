# 项目目录说明

本项目当前采用“桌面客户端 + Web 前端 + Java 后端 + 设计文档”的组织方式，目录结构说明如下。

## 根目录文件

- `开发.md`
  - 项目开发说明、流程图说明、技术选型和架构说明
- `需求.md`
  - 项目需求描述
- `系统结构图.puml`
  - 系统整体结构图
- `系统类图.puml`
  - 系统总览类图
- `程序流程图.puml`
  - 程序主流程图，按客户端与服务端分区展示
- `客户端类图.puml`
  - 客户端类图
- `服务端类图.puml`
  - 服务端类图

## 桌面客户端目录

- `desktop-client/`
  - Windows 桌面客户端工程目录
- `desktop-client/MyCloud.Desktop.sln`
  - Visual Studio 解决方案文件
- `desktop-client/MyCloud.Desktop/`
  - WinUI 3 + C++ 客户端主工程目录

### 桌面客户端主要子目录

- `App.xaml`
  - 应用入口配置文件
- `MainWindow.xaml`
  - 主窗口页面
- `Views/`
  - 页面视图目录
  - 包含 `LoginPage`、`HomePage`、`UploadPage`、`SharePage`、`FileManagerPage`
- `ViewModels/`
  - 视图模型目录
  - 当前包含 `LoginViewModel`、`FileViewModel`
- `Services/`
  - 客户端服务层目录
  - 当前包含 `ApiService`、`AuthService`、`FileService`
- `Models/`
  - 客户端数据模型目录
  - 当前包含 `User`、`FileItem`、`ShareRecord`

## Web 前端目录

- `web-frontend/`
  - Vue 3 前端项目目录
- `package.json`
  - 前端项目配置与脚本入口
- `vite.config.js`
  - Vite 构建配置
- `index.html`
  - 前端页面宿主文件

### Web 前端主要子目录

- `src/main.js`
  - 前端入口文件
- `src/App.vue`
  - 前端根组件
- `src/api/`
  - 接口请求封装目录
- `src/router/`
  - 路由配置目录
- `src/stores/`
  - 状态管理目录
- `src/views/`
  - 页面视图目录
  - 当前包含 `LoginView`、`HomeView`、`UploadView`、`ShareView`、`FileManagerView`
- `src/components/`
  - 公共组件目录
  - 当前包含 `FileTable`、`ShareDialog`

## 服务端目录

- `server/`
  - Java Spring Boot 后端项目目录
- `pom.xml`
  - Maven 项目配置文件

### 服务端主要子目录

- `src/main/java/com/mycloud/`
  - 后端主代码目录
- `MyCloudApplication.java`
  - 后端启动入口
- `controller/`
  - 控制器目录
  - 当前包含 `AuthController`、`FileController`、`ShareController`
- `service/`
  - 业务服务目录
  - 当前包含 `AuthService`、`FileService`、`ShareService`
- `entity/`
  - 实体类目录
  - 当前包含 `User`、`FileEntity`、`ShareRecord`
- `repository/`
  - 数据访问接口目录
  - 当前包含 `UserRepository`、`FileRepository`、`ShareRepository`
- `config/`
  - 配置类目录
  - 当前包含 `SecurityConfig`
- `src/main/resources/`
  - 资源与配置目录
  - 当前包含 `application.yml`
- `src/test/java/com/mycloud/`
  - 测试代码目录
  - 当前包含 `MyCloudApplicationTests`

## 当前结构特点

- 设计文档和程序骨架放在同一仓库中，便于边设计边实现
- 桌面端与 Web 端共用同一个 Java 后端
- 当前所有代码文件仅为基础模板，尚未进入具体业务实现阶段

## 后续建议

- 在 `desktop-client/` 中继续补充页面导航和服务调用逻辑
- 在 `web-frontend/` 中先完成路由、登录页和主页基础布局
- 在 `server/` 中先补齐 Spring Boot 依赖和最小可启动接口
