# 详细设计-接口设计

## 1. 通用约定

### 1.1 请求头

需要登录的接口必须携带：

```text
Authorization: Bearer <jwt>
X-Client-Type: web | windows | android
```

### 1.2 响应格式

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

分页格式：

```json
{
  "items": [],
  "page": 1,
  "size": 20,
  "total": 100
}
```

### 1.3 文件对象

```json
{
  "id": 1,
  "parentId": null,
  "name": "docs",
  "type": "folder",
  "mimeType": null,
  "size": 0,
  "extension": null,
  "createdAt": "2026-06-20T10:00:00",
  "updatedAt": "2026-06-20T10:00:00"
}
```

## 2. 用户接口

### 2.1 注册

```text
POST /api/auth/register
```

请求：

```json
{
  "username": "alice",
  "email": "alice@example.com",
  "password": "Password123",
  "confirmPassword": "Password123"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userId": 1,
    "username": "alice"
  }
}
```

### 2.2 登录

```text
POST /api/auth/login
```

请求：

```json
{
  "account": "alice",
  "password": "Password123"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "<jwt>",
    "expiresIn": 604800,
    "user": {
      "id": 1,
      "username": "alice",
      "email": "alice@example.com",
      "role": "user",
      "totalCapacity": 10737418240,
      "usedCapacity": 0,
      "status": "normal"
    }
  }
}
```

### 2.3 退出登录

```text
POST /api/auth/logout
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

### 2.4 获取用户信息

```text
GET /api/user/profile
```

响应字段同登录响应中的 `user`。

## 3. 文件接口

### 3.1 获取文件列表

```text
GET /api/files?parentId=&page=1&size=20
```

参数：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| parentId | 否 | 父目录 ID，空表示根目录 |
| page | 否 | 页码，默认 1 |
| size | 否 | 每页数量，默认 20 |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [],
    "page": 1,
    "size": 20,
    "total": 0
  }
}
```

### 3.2 上传文件

```text
POST /api/files/upload
Content-Type: multipart/form-data
```

表单字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| parentId | 否 | 目标目录 ID |
| file | 是 | 文件内容 |
| conflictPolicy | 否 | auto_rename 或 reject，默认 auto_rename |

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "file": {
      "id": 10,
      "parentId": null,
      "name": "demo.zip",
      "type": "file",
      "mimeType": "application/zip",
      "size": 1024,
      "extension": "zip",
      "createdAt": "2026-06-20T10:00:00",
      "updatedAt": "2026-06-20T10:00:00"
    },
    "taskId": 100
  }
}
```

### 3.3 下载文件

```text
GET /api/files/{id}/download
```

响应：

- 成功时返回文件二进制流。
- 失败时返回统一 JSON 错误。

响应头：

```text
Content-Type: <mimeType>
Content-Length: <size>
Content-Disposition: attachment; filename="<encodedName>"
```

### 3.4 新建文件夹

```text
POST /api/folders
```

请求：

```json
{
  "parentId": null,
  "name": "资料"
}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 11,
    "parentId": null,
    "name": "资料",
    "type": "folder",
    "size": 0
  }
}
```

### 3.5 重命名

```text
PATCH /api/files/{id}/rename
```

请求：

```json
{
  "name": "new-name.zip"
}
```

响应：返回更新后的文件对象。

### 3.6 移动

```text
PATCH /api/files/{id}/move
```

请求：

```json
{
  "targetParentId": 20
}
```

响应：返回更新后的文件对象。

### 3.7 删除

```text
DELETE /api/files/{id}
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": null
}
```

## 4. 搜索接口

```text
GET /api/search/files?keyword=doc&type=file&startTime=2026-01-01T00:00:00&endTime=2026-12-31T23:59:59&page=1&size=20
```

参数：

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| keyword | 否 | 文件名关键词 |
| type | 否 | file 或 folder |
| startTime | 否 | 更新时间开始 |
| endTime | 否 | 更新时间结束 |
| page | 否 | 页码 |
| size | 否 | 每页数量 |

响应：分页文件对象。

## 5. 任务接口

### 5.1 获取任务列表

```text
GET /api/tasks?status=failed&page=1&size=20
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [
      {
        "id": 1,
        "fileId": 10,
        "taskType": "upload",
        "clientType": "web",
        "status": "success",
        "progress": 100,
        "errorMessage": null,
        "createdAt": "2026-06-20T10:00:00",
        "updatedAt": "2026-06-20T10:01:00"
      }
    ],
    "page": 1,
    "size": 20,
    "total": 1
  }
}
```

### 5.2 获取任务详情

```text
GET /api/tasks/{id}
```

响应：单个任务对象。

### 5.3 取消任务

```text
POST /api/tasks/{id}/cancel
```

响应：取消后的任务对象。

### 5.4 重试任务

```text
POST /api/tasks/{id}/retry
```

响应：

```json
{
  "code": 0,
  "message": "请重新发起上传或下载请求",
  "data": {
    "taskId": 1,
    "status": "failed"
  }
}
```

## 6. 管理员接口

### 6.1 用户列表

```text
GET /api/admin/users?keyword=alice&page=1&size=20
```

响应：分页用户对象。

### 6.2 用户详情

```text
GET /api/admin/users/{id}
```

响应：用户详情对象。

### 6.3 修改用户状态

```text
PATCH /api/admin/users/{id}/status
```

请求：

```json
{
  "status": "disabled"
}
```

### 6.4 修改用户容量

```text
PATCH /api/admin/users/{id}/capacity
```

请求：

```json
{
  "totalCapacity": 21474836480
}
```

### 6.5 存储统计

```text
GET /api/admin/storage
```

响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "userCount": 10,
    "fileCount": 100,
    "folderCount": 20,
    "totalUsedCapacity": 104857600
  }
}
```

### 6.6 操作日志

```text
GET /api/admin/logs?userId=1&action=upload&page=1&size=20
```

响应：分页日志对象。
