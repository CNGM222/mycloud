# 程序流程图说明

当前仓库里还没有可直接追踪的源码实现，所以这版流程图先按 `MyCloud` 的典型业务流程进行系统级映射。当前版本已经按职责拆成 `客户端` 和 `服务端` 两个分区。

对应流程图文件： [程序流程图.puml](/mycloud/程序流程图.puml)

对应拆分类图文件：

- [客户端类图.puml](/mycloud/客户端类图.puml)
- [服务端类图.puml](/mycloud/服务端类图.puml)

## 分层说明

- `客户端`：负责页面展示、用户输入采集、请求发起、结果展示
- `服务端`：负责认证、权限校验、业务处理、文件存储协调、日志记录
- 这次的“服务端”按你的要求采用“集成前后端业务”的表达方式，也就是把浏览器之外的业务能力统一放在服务端分区里展示
- 原有 `系统类图.puml` 可以继续保留为总览图，新的两张类图用于分层展开

## 当前覆盖的主流程

- 用户登录与注册
- 文件上传、下载、预览
- 文件分享
- 文件管理
- 会话退出

## 建模假设

- 系统是一个面向用户的云盘/网盘类应用
- 所有业务操作都先经过身份认证与权限校验
- 文件操作会同步更新元数据，并记录操作日志
- 目前展示的是主干流程，还没有展开异常补偿、分片上传、回收站、秒传等细节

## 后续可以继续细化的方向

- 把“上传文件”拆成分片上传流程
- 把“分享文件”拆成公开分享与指定用户分享
- 把“删除文件”补成回收站流程
- 把“登录”补成验证码、找回密码、多端登录控制

## 技术选型

本系统采用“双客户端 + 统一服务端”的实现思路：

- 桌面客户端：`C++ + WinUI 3`
- Web 前端：`Vue 3`
- 后端服务：`Java + Spring Boot`

## 技术架构说明

- `C++/WinUI 3` 负责 Windows 桌面客户端，适合本地文件选择、上传下载、系统交互等桌面场景
- `Vue 3` 负责浏览器端页面展示与交互，适合实现登录、文件列表、分享管理等 Web 页面
- `Java Spring Boot` 负责统一后端接口，为桌面端和 Web 端同时提供认证、文件管理、分享管理、权限校验等服务
- 两个客户端共用同一套后端业务逻辑和数据存储，避免重复开发

## 推荐配套组件

- 桌面客户端：`C++`、`WinUI 3`
- Web 前端：`Vue 3`、`Vue Router`、`Pinia`、`Axios`
- 后端：`Spring Boot`、`Spring Security`、`JWT`
- 数据访问：`MyBatis-Plus` 或 `JPA`
- 数据库：`MySQL`
- 文件存储：本地磁盘存储，后续可扩展 `MinIO`

## 架构优势

- 同时支持 Windows 桌面端和浏览器访问
- 后端统一管理认证、文件和分享业务，系统结构更清晰
- Web 端开发效率高，桌面端具备更好的本地系统集成能力
- 后续扩展移动端或小程序时，可以继续复用现有 Java 后端接口
