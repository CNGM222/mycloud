# 个人网盘系统概要设计

## 1. 引言

### 1.1 编写目的

本文档根据《需求文档.md》编写，用于描述个人网盘系统的总体设计方案。文档采用瀑布模型的软件工程思路，在需求分析之后，对系统架构、模块划分、数据结构、接口设计、部署方式和测试要点进行概要说明，为后续详细设计、编码实现、测试验收和项目交付提供依据。

### 1.2 项目范围

本系统为个人网盘系统，采用一个统一后端同时支持三种访问端：

- B/S 网页端：Vue 3。
- Windows 客户端：C/S 客户端。
- Android 客户端：C/S 客户端。

后端使用 Java + Spring Boot 开发，通过 JDBC 访问 MySQL 9.6 数据库。用户数据、文件目录、文件元数据、容量、任务记录和操作日志存储在 MySQL 中，文件二进制内容存储在后端服务器本地文件系统中。

系统不包含以下功能：

- 文件在线预览。
- 文件分享。
- 回收站。
- 文件系统内恢复。
- macOS 客户端。
- Linux 客户端。

### 1.3 设计原则

- 统一后端：网页端、Windows 客户端和 Android 客户端共用同一套 REST API。
- 分层设计：表现层、接口层、业务层、数据访问层和数据库层职责清晰。
- 模块化：按照用户、文件、搜索、容量、任务、管理员和日志等业务领域划分模块，降低模块之间的耦合度。
- 权限优先：所有文件相关操作必须校验用户身份和文件归属。
- 数据一致：文件元数据、文件内容和用户容量变更必须保持一致。
- 流式传输：文件上传和下载通过 Java 输入输出流分块处理，避免大文件一次性进入内存。
- 可维护性：模块边界清晰，便于后续详细设计和编码实现。

## 2. 瀑布模型开发流程设计

本项目按照瀑布模型组织开发活动，各阶段顺序推进，每个阶段输出明确文档或成果物。

### 2.1 需求分析阶段

输入：

- 用户对个人网盘系统的功能要求。
- 技术选型要求。
- 平台范围要求。

输出：

- 需求文档.md。

主要内容：

- 明确系统功能范围。
- 明确 B/S 和 C/S 交互方式。
- 明确 Java Spring Boot、Vue 3、Windows、Android、MySQL 9.6、JDBC 等技术约束。

### 2.2 概要设计阶段

输入：

- 需求文档.md。

输出：

- 概要设计.md。

主要内容：

- 系统总体架构。
- 功能模块划分。
- 数据库概要结构。
- 主要接口设计。
- 安全、性能、部署和测试策略。

### 2.3 详细设计阶段

后续应输出详细设计文档。

主要内容：

- 数据库建表 SQL。
- 后端类图、包结构和核心流程。
- 前端页面组件结构。
- Windows 客户端界面和业务流程。
- Android 客户端界面和业务流程。
- 接口请求和响应字段明细。
- 关键异常处理逻辑。

### 2.4 编码实现阶段

根据详细设计进行编码。

主要产物：

- Spring Boot 后端项目。
- Vue 3 网页端项目。
- Windows 客户端项目。
- Android 客户端项目。
- 数据库初始化脚本。

### 2.5 测试阶段

根据需求文档和设计文档进行测试。

测试范围：

- 单元测试。
- 接口测试。
- 功能测试。
- 权限测试。
- 文件上传下载测试。
- 三端数据一致性测试。
- 性能和异常场景测试。

### 2.6 部署与维护阶段

完成系统部署、运行配置和维护说明。

主要内容：

- 后端服务部署。
- MySQL 数据库部署。
- Vue 3 网页端部署。
- Windows 客户端打包。
- Android 客户端打包。
- 数据库备份和恢复策略。
- 日志查看和故障处理说明。

## 3. 系统总体架构

### 3.1 架构模式

系统采用前后端分离架构和统一后端架构。

整体结构如下：

```text
用户
 |
 +-- 浏览器 / Vue 3 网页端
 |
 +-- Windows 客户端
 |
 +-- Android 客户端
        |
        v
 Java Spring Boot 后端服务
        |
        +-- JDBC / HikariCP --> MySQL 9.6 数据库
        |                       保存文件元数据和业务数据
        |
        +-- Java NIO ---------> 服务器本地文件系统
                                保存文件二进制内容
```

### 3.2 分层结构

后端采用典型分层结构：

```text
Controller 层：接收 HTTP 请求，参数校验，返回统一响应
Service 层：处理核心业务逻辑和事务控制
DAO 层：通过 JDBC 执行数据库访问
Model 层：定义实体对象、DTO、VO 和枚举
Database 层：保存用户、文件元数据、容量、任务和日志数据
Storage 层：在受控的服务器本地目录中流式保存和读取文件内容
```

客户端与网页端结构：

```text
页面 / 窗口层：展示界面，接收用户操作
状态管理层：维护登录状态、文件列表、任务状态
API 调用层：封装 HTTP 请求
本地存储层：保存必要的登录凭证和客户端配置
```

## 4. 技术架构设计

### 4.1 后端技术

- 开发语言：Java。
- 后端框架：Spring Boot。
- Web 框架：Spring MVC。
- 数据库访问：JDBC。
- 数据库连接池：HikariCP。
- 数据库：MySQL 9.6。
- 接口风格：REST API。
- 文件传输：HTTP multipart 上传，HTTP stream 下载。

### 4.2 网页端技术

- 前端框架：Vue 3。
- 构建工具：Vite。
- 开发语言：TypeScript。
- 请求工具：Axios 或 Fetch。
- UI 组件库：Element Plus 或其他 Vue 3 组件库。

### 4.3 Windows 客户端技术

Windows 客户端可采用 Electron + Vue 3、JavaFX 或 WPF。概要设计推荐 Electron + Vue 3，便于复用网页端开发经验和部分界面组件。

主要职责：

- 调用后端 REST API。
- 管理用户登录状态。
- 展示文件列表。
- 上传和下载文件。
- 维护本地任务进度。

### 4.4 Android 客户端技术

Android 客户端推荐使用 Kotlin 或 Java 原生开发。

主要职责：

- 调用后端 REST API。
- 管理用户登录状态。
- 展示移动端文件列表。
- 上传和下载手机本地文件。
- 适配触控交互和 Android 文件访问权限。

## 5. 功能模块设计

## 5.1 用户认证模块

### 5.1.1 模块职责

- 用户注册。
- 用户登录。
- 用户退出登录。
- 登录状态校验。
- 用户基础信息查询。
- 用户账号状态判断。

### 5.1.2 主要流程

注册流程：

```text
客户端提交注册信息
 -> 后端校验用户名和邮箱
 -> 密码加密
 -> 写入用户表
 -> 返回注册结果
```

登录流程：

```text
客户端提交账号和密码
 -> 后端查询用户
 -> 校验密码
 -> 校验账号状态
 -> 生成登录凭证
 -> 返回用户信息和登录凭证
```

### 5.1.3 设计要点

- 密码只能保存哈希值。
- 登录失败不能暴露用户是否存在等敏感细节。
- 登录凭证需要具备过期机制。
- Windows 和 Android 客户端本地保存凭证时需要使用安全存储方式。

## 5.2 文件管理模块

### 5.2.1 模块职责

- 查询文件列表。
- 上传文件。
- 下载文件。
- 新建文件夹。
- 重命名文件或文件夹。
- 移动文件或文件夹。
- 删除文件或文件夹。
- 校验文件归属权限。

### 5.2.2 文件上传概要流程

```text
客户端选择文件
 -> 发起 multipart 上传请求
 -> 后端校验登录凭证
 -> 校验目标目录权限
 -> 校验文件名、文件大小和剩余容量
 -> 创建任务记录
 -> 流式写入本地临时文件并计算哈希
 -> 原子移动到正式存储目录
 -> 写入文件元数据和相对存储键
 -> 更新用户已用容量
 -> 记录操作日志
 -> 返回上传结果
```

### 5.2.3 文件下载概要流程

```text
客户端请求下载文件
 -> 后端校验登录凭证
 -> 校验文件归属权限
 -> 查询文件元数据
 -> 根据相对存储键流式读取本地文件
 -> 通过 HTTP 响应流返回文件
 -> 记录操作日志
```

### 5.2.4 删除概要流程

```text
客户端请求删除文件或文件夹
 -> 后端校验登录凭证
 -> 校验文件归属权限
 -> 查询待删除节点及子节点
 -> 将本地文件移动到隔离回收目录
 -> 删除文件元数据记录
 -> 更新用户已用容量
 -> 数据库提交后清理隔离文件
 -> 记录操作日志
 -> 返回删除结果
```

### 5.2.5 设计要点

- 文件夹使用文件表中的 type 字段区分。
- parent_id 表示目录层级关系。
- 删除文件夹时需要递归处理所有子文件和子文件夹。
- 移动文件夹时必须防止移动到自身或子目录。
- 本地物理文件使用后端生成的随机存储键，不使用用户文件名作为物理路径。
- 文件重命名和移动只修改逻辑目录元数据，不移动物理文件。
- 上传和下载必须流式处理，避免一次性加载完整文件。

## 5.3 搜索模块

### 5.3.1 模块职责

- 按文件名模糊搜索。
- 按文件类型筛选。
- 按时间范围筛选。
- 返回当前用户有权限访问的搜索结果。

### 5.3.2 设计要点

- 搜索必须附带 user_id 条件，防止越权。
- 常用搜索字段需要建立索引。
- 搜索结果应分页返回。
- 三端使用同一搜索接口，保证结果一致。

## 5.4 容量管理模块

### 5.4.1 模块职责

- 维护用户总容量。
- 维护用户已用容量。
- 上传前检查剩余容量。
- 删除后释放容量。
- 管理员调整用户容量。

### 5.4.2 设计要点

- 上传文件、删除文件和调整容量需要使用事务。
- used_capacity 应与 `files` 表中有效文件的 size 汇总保持一致。
- 可提供定期校验工具，用于检查容量统计是否异常。

## 5.5 任务管理模块

### 5.5.1 模块职责

- 记录上传任务。
- 记录下载任务。
- 查询任务列表。
- 查询任务详情。
- 取消任务。
- 重试失败任务。

### 5.5.2 设计要点

- 任务状态包括 pending、running、success、failed、cancelled。
- 网页端、Windows 客户端和 Android 客户端均可展示任务状态。
- 客户端可维护本地实时进度，服务端保存任务最终状态和关键记录。

## 5.6 管理员模块

### 5.6.1 模块职责

- 查看用户列表。
- 查看用户详情。
- 禁用或恢复用户账号。
- 调整用户容量。
- 查看系统存储统计。
- 查看操作日志。

### 5.6.2 设计要点

- 管理员接口必须校验管理员角色。
- 管理员调整容量不能导致用户当前已用容量超过总容量后仍继续上传。
- 管理员操作需要写入操作日志。

## 5.7 操作日志模块

### 5.7.1 模块职责

- 记录用户关键操作。
- 记录管理员关键操作。
- 支持按用户、操作类型、客户端类型、时间范围查询日志。

### 5.7.2 记录范围

- 登录。
- 退出登录。
- 上传文件。
- 下载文件。
- 新建文件夹。
- 重命名。
- 移动。
- 删除。
- 管理员修改用户状态。
- 管理员调整容量。

## 6. 数据库与文件存储概要设计

## 6.1 数据库设计原则

- 用户、文件元数据、目录关系、容量、任务和日志存储在 MySQL 9.6。
- 后端通过 JDBC 访问数据库，前端和客户端不得直连数据库。
- 文件二进制内容存储在服务器本地文件系统，数据库保存相对存储键，不保存绝对路径或 BLOB。
- 元数据、容量、任务和日志变更放在数据库事务中；文件系统操作通过临时文件、原子移动、失败补偿和巡检任务协调。
- 常用查询字段建立索引。

## 6.2 核心数据表

### 6.2.1 users 用户表

| 字段名 | 类型建议 | 说明 |
| --- | --- | --- |
| id | BIGINT | 用户 ID |
| username | VARCHAR(64) | 用户名，唯一 |
| email | VARCHAR(128) | 邮箱，唯一 |
| password_hash | VARCHAR(255) | 密码哈希 |
| role | VARCHAR(32) | user 或 admin |
| total_capacity | BIGINT | 总容量，单位字节 |
| used_capacity | BIGINT | 已用容量，单位字节 |
| status | VARCHAR(32) | normal 或 disabled |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.2.2 files 文件表

| 字段名 | 类型建议 | 说明 |
| --- | --- | --- |
| id | BIGINT | 文件 ID |
| user_id | BIGINT | 所属用户 ID |
| parent_id | BIGINT | 父目录 ID，根目录为空 |
| name | VARCHAR(255) | 文件或文件夹名称 |
| type | VARCHAR(16) | file 或 folder |
| mime_type | VARCHAR(128) | MIME 类型 |
| size | BIGINT | 文件大小，文件夹为 0 |
| storage_key | VARCHAR(512) | 后端生成的相对存储键，文件夹为空 |
| storage_status | VARCHAR(16) | active、deleting 或 error，文件夹为空 |
| md5 | VARCHAR(64) | 文件哈希 |
| extension | VARCHAR(32) | 扩展名 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.2.3 tasks 任务记录表

| 字段名 | 类型建议 | 说明 |
| --- | --- | --- |
| id | BIGINT | 任务 ID |
| user_id | BIGINT | 用户 ID |
| file_id | BIGINT | 文件 ID |
| task_type | VARCHAR(16) | upload 或 download |
| client_type | VARCHAR(16) | web、windows 或 android |
| status | VARCHAR(32) | 任务状态 |
| progress | INT | 任务进度 |
| error_message | VARCHAR(512) | 错误信息 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 6.2.4 operation_logs 操作日志表

| 字段名 | 类型建议 | 说明 |
| --- | --- | --- |
| id | BIGINT | 日志 ID |
| user_id | BIGINT | 用户 ID |
| action | VARCHAR(64) | 操作类型 |
| target_id | BIGINT | 操作对象 ID |
| target_type | VARCHAR(32) | 操作对象类型 |
| client_type | VARCHAR(16) | web、windows 或 android |
| ip_address | VARCHAR(64) | IP 地址 |
| created_at | DATETIME | 创建时间 |

## 6.3 索引设计

建议索引：

- users.username 唯一索引。
- users.email 唯一索引。
- files.user_id 普通索引。
- files.parent_id 普通索引。
- files.user_id + files.parent_id + files.name 联合唯一索引。
- files.user_id + files.name 普通索引，用于搜索。
- files.storage_key 唯一索引，保证物理文件映射唯一。
- tasks.user_id + tasks.created_at 普通索引。
- operation_logs.user_id + operation_logs.created_at 普通索引。

## 6.4 数据一致性设计

上传文件协调流程：

```text
流式写入本地临时文件并校验大小、MD5
 -> 原子移动到正式存储路径
 -> 开启数据库事务并插入文件元数据和 storage_key
 -> 更新用户 used_capacity
 -> 插入任务记录和操作日志
 -> 提交数据库事务
 -> 失败时删除已落盘文件，清理失败则交由巡检任务处理
```

删除文件协调流程：

```text
查询待删除文件
 -> 数据库将相关文件标记为 deleting，阻止新的下载
 -> 将物理文件原子移动到隔离回收目录
 -> 开启数据库事务删除文件元数据
 -> 更新用户 used_capacity
 -> 插入操作日志
 -> 提交数据库事务
 -> 清理隔离回收目录；失败时恢复物理文件并将状态改回 active 或标记 error
```

数据库步骤失败时必须回滚。由于数据库事务不能覆盖本地文件系统操作，后端必须执行补偿动作，并定期巡检孤立文件、缺失文件和异常状态记录。

## 7. 接口概要设计

### 7.1 接口通用约定

请求头：

```text
Authorization: Bearer <token>
X-Client-Type: web | windows | android
```

统一响应格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

错误响应示例：

```json
{
  "code": 40001,
  "message": "未登录或登录已过期",
  "data": null
}
```

### 7.2 用户接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/logout | 退出登录 |
| GET | /api/user/profile | 获取用户信息 |

### 7.3 文件接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/files | 获取文件列表 |
| POST | /api/files/upload | 上传文件 |
| GET | /api/files/{id}/download | 下载文件 |
| POST | /api/folders | 新建文件夹 |
| PATCH | /api/files/{id}/rename | 重命名文件或文件夹 |
| PATCH | /api/files/{id}/move | 移动文件或文件夹 |
| DELETE | /api/files/{id} | 删除文件或文件夹 |

### 7.4 搜索接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/search/files | 搜索文件和文件夹 |

### 7.5 任务接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/tasks | 获取任务列表 |
| GET | /api/tasks/{id} | 获取任务详情 |
| POST | /api/tasks/{id}/cancel | 取消任务 |
| POST | /api/tasks/{id}/retry | 重试任务 |

### 7.6 管理员接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | /api/admin/users | 获取用户列表 |
| GET | /api/admin/users/{id} | 获取用户详情 |
| PATCH | /api/admin/users/{id}/status | 修改用户状态 |
| PATCH | /api/admin/users/{id}/capacity | 修改用户容量 |
| GET | /api/admin/storage | 获取系统存储统计 |
| GET | /api/admin/logs | 获取操作日志 |

## 8. 客户端概要设计

## 8.1 Vue 3 网页端

页面结构：

- 登录页。
- 注册页。
- 网盘首页。
- 用户信息页。
- 管理员页面。

主要组件：

- 文件列表组件。
- 文件上传组件。
- 路径面包屑组件。
- 容量展示组件。
- 搜索组件。
- 任务进度组件。
- 用户菜单组件。

状态管理：

- 登录用户信息。
- token。
- 当前目录 ID。
- 当前文件列表。
- 搜索条件。
- 上传和下载任务状态。

## 8.2 Windows 客户端

窗口结构：

- 登录窗口。
- 主窗口。
- 任务窗口。

主要能力：

- 调用统一 REST API。
- 保存登录凭证。
- 选择本地文件上传。
- 选择本地目录下载。
- 展示远程目录树和文件列表。
- 展示上传下载任务进度。

## 8.3 Android 客户端

页面结构：

- 登录页。
- 文件首页。
- 搜索页。
- 任务页。
- 用户信息页。

主要能力：

- 调用统一 REST API。
- 保存登录凭证。
- 申请本地文件访问权限。
- 上传本地文件。
- 下载文件到设备可访问目录。
- 适配触控操作和小屏幕布局。

## 9. 安全设计

### 9.1 身份认证

- 登录成功后由后端签发登录凭证。
- 需要登录的接口必须校验登录凭证。
- 退出登录后凭证应失效。

### 9.2 权限控制

- 普通用户只能访问自己的文件和任务。
- 管理员接口只能由管理员访问。
- 文件下载、删除、移动、重命名必须校验文件归属。

### 9.3 密码安全

- 密码不得明文存储。
- 密码哈希需要使用安全算法。
- 登录失败应返回统一错误提示。

### 9.4 数据库安全

- JDBC 连接账号遵循最小权限原则。
- 数据库连接信息只保存在后端配置中。
- 前端和客户端不得保存数据库连接信息。

### 9.5 客户端安全

- Windows 和 Android 客户端需要安全保存登录凭证。
- 客户端不得保存用户密码明文。
- 网络请求应使用 HTTPS。

## 10. 性能设计

### 10.1 文件列表性能

- 文件列表分页查询。
- 目录查询通过 user_id 和 parent_id 索引加速。
- 搜索接口分页返回结果。

### 10.2 文件上传下载性能

- 上传时通过流式方式写入本地临时文件。
- 下载时通过流式方式读取本地文件。
- 避免一次性读取完整文件到内存。
- 对单文件大小和并发上传数量进行限制。

### 10.3 数据库性能

- 使用 HikariCP 管理 JDBC 连接池。
- 常用字段建立索引。
- 数据库不保存文件二进制内容，避免大文件 BLOB 对连接池、备份体积和恢复时间造成压力。

### 10.4 本地存储性能

- 存储根目录应部署在容量充足、支持监控和备份的独立磁盘或挂载卷。
- 正式文件按存储键前缀分层目录保存，避免单目录文件数量过大。
- 临时目录、正式目录和隔离回收目录应位于同一文件系统，以支持原子移动。

## 11. 异常处理设计

### 11.1 通用异常

- 参数错误。
- 未登录。
- 登录已过期。
- 权限不足。
- 资源不存在。
- 数据库访问失败。
- 系统内部错误。

### 11.2 文件相关异常

- 文件名重复。
- 文件大小超过限制。
- 用户容量不足。
- 目标目录不存在。
- 不能移动到自身或子目录。
- 上传中断。
- 下载失败。

### 11.3 异常响应原则

- 后端返回统一错误格式。
- 错误信息对用户友好。
- 敏感内部异常只写入服务端日志，不直接暴露给客户端。

## 12. 部署概要设计

### 12.1 部署组成

```text
Nginx 或 Web 服务器：部署 Vue 3 静态资源
Spring Boot 服务：部署后端 API
MySQL 9.6：部署系统数据库
本地存储卷：保存文件二进制内容、临时文件和隔离回收文件
Windows 安装包：分发 Windows 客户端
Android APK：分发 Android 客户端
```

### 12.2 后端部署

- Spring Boot 打包为 jar 或容器镜像。
- 配置数据库连接地址、账号、密码和连接池参数。
- 配置上传文件大小限制。
- 配置本地存储根目录、临时目录、隔离回收目录和最小剩余空间阈值。
- 配置日志输出路径。

### 12.3 数据库部署

- 初始化数据库和表结构。
- 创建后端专用数据库账号。
- 配置备份策略。
- 监控数据库容量和元数据增长。

### 12.4 本地文件存储部署

- 存储目录只能由后端服务账号读写，不允许 Web 服务器直接暴露。
- 存储卷需要独立进行容量监控、备份和恢复演练。
- 数据库备份与文件存储快照应使用同一备份批次标识，恢复后执行一致性巡检。

### 12.5 客户端部署

- Windows 客户端打包为安装程序。
- Android 客户端打包为 APK。
- 客户端配置后端服务地址。

## 13. 测试概要设计

### 13.1 单元测试

- 用户注册和登录逻辑。
- 文件名校验逻辑。
- 容量计算逻辑。
- 文件移动合法性判断。
- 权限校验逻辑。

### 13.2 接口测试

- 用户接口。
- 文件接口。
- 搜索接口。
- 任务接口。
- 管理员接口。

### 13.3 功能测试

- 注册、登录、退出。
- 上传、下载、删除、重命名、移动。
- 新建文件夹和进入文件夹。
- 搜索文件。
- 管理员禁用用户。
- 管理员调整容量。

### 13.4 三端一致性测试

- 网页端上传，Windows 和 Android 客户端可见。
- Windows 客户端上传，网页端和 Android 客户端可见。
- Android 客户端上传，网页端和 Windows 客户端可见。
- 任一端删除文件，其他端刷新后不可见。

### 13.5 安全测试

- 未登录访问接口。
- 普通用户访问管理员接口。
- 用户访问其他用户文件。
- 登录凭证过期。
- SQL 注入防护。

### 13.6 性能测试

- 大文件上传。
- 大文件下载。
- 多用户并发上传。
- 大目录文件列表分页。
- 文件名搜索。

## 14. 需求追踪矩阵

| 需求类别 | 对应设计章节 |
| --- | --- |
| 用户注册、登录、退出 | 5.1、7.2、9.1 |
| 文件上传、下载、删除、重命名、移动 | 5.2、7.3、10.2 |
| 文件夹管理 | 5.2、7.3 |
| 搜索文件 | 5.3、7.4、10.1 |
| 容量管理 | 5.4、6.2、6.4 |
| 上传下载任务 | 5.5、7.5、8 |
| 管理员功能 | 5.6、7.6 |
| 操作日志 | 5.7、6.2.4 |
| Vue 3 网页端 | 8.1 |
| Windows 客户端 | 8.2 |
| Android 客户端 | 8.3 |
| MySQL 9.6 和 JDBC | 4.1、6、10.3 |

## 15. 后续工作

概要设计完成后，下一步进入详细设计阶段。详细设计需要进一步明确：

- 数据库建表 SQL。
- 后端包结构、类结构和核心方法。
- REST API 请求参数和响应字段。
- Vue 3 页面路由和组件结构。
- Windows 客户端窗口布局和交互细节。
- Android 客户端页面布局和权限申请流程。
- 文件上传下载的本地文件流、原子移动和补偿清理实现细节。
- 测试用例清单。
