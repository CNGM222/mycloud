# 个人网盘项目需求文档

## 1. 项目概述

### 1.1 项目名称

个人网盘系统

### 1.2 项目背景

用户在日常学习、办公和生活中会产生大量文件，例如文档、图片、视频、压缩包、代码资料等。传统本地存储容易受到设备损坏、空间不足、文件查找困难、多设备访问不便等问题影响。

本项目旨在开发一个面向个人使用的网盘系统，提供文件上传、下载、分类管理、搜索、文件夹管理、容量统计等核心能力。系统采用一个统一后端，同时支持 B/S 和 C/S 两种交互方式：

- B/S：用户通过浏览器访问 Vue 3 网页端管理文件。
- C/S：用户通过 Windows 客户端或 Android 客户端连接后端管理文件。

### 1.3 项目目标

- 提供稳定可靠的个人文件存储与管理能力。
- 支持文件上传、下载、删除、重命名、移动、搜索和文件夹管理。
- 使用同一个 Java Spring Boot 后端服务同时支持网页端、Windows 客户端和 Android 客户端。
- 使用统一身份认证和权限控制，保护个人文件安全。
- 使用 MySQL 9.6 数据库存储系统数据和文件内容，并通过 JDBC 接口完成数据库访问。
- 直接面向最终版本开发，不再拆分 MVP 或阶段版本规划。

## 2. 系统形态

### 2.1 B/S 网页端

B/S 即 Browser/Server，用户通过浏览器访问网页端。

技术要求：

- 前端使用 Vue 3。
- 通过 HTTP API 与 Spring Boot 后端交互。
- 支持主流现代浏览器。
- 页面主要面向桌面浏览器使用。

主要能力：

- 用户注册、登录、退出登录。
- 查看网盘文件和文件夹。
- 上传文件。
- 下载文件。
- 新建文件夹。
- 重命名文件或文件夹。
- 移动文件或文件夹。
- 删除文件或文件夹。
- 搜索文件。
- 查看容量使用情况。

### 2.2 Windows 客户端

Windows 客户端属于 C/S 交互方式，用户通过安装在 Windows 系统上的客户端程序访问后端服务。

主要能力：

- 用户登录和退出登录。
- 记住登录状态。
- 查看远程文件列表。
- 上传本地文件。
- 下载远程文件。
- 新建、重命名、移动、删除文件或文件夹。
- 搜索文件。
- 查看上传和下载任务进度。
- 选择本地下载目录。

### 2.3 Android 客户端

Android 客户端属于 C/S 交互方式，用户通过 Android 手机或平板访问后端服务。

主要能力：

- 用户登录和退出登录。
- 记住登录状态。
- 查看远程文件列表。
- 上传本地文件。
- 下载远程文件。
- 新建、重命名、移动、删除文件或文件夹。
- 搜索文件。
- 查看上传和下载任务进度。
- 适配移动端触控操作。

### 2.4 统一后端服务

系统只维护一个后端服务，Vue 3 网页端、Windows 客户端和 Android 客户端都通过同一套后端 API 访问数据。

后端职责：

- 用户认证。
- 权限校验。
- 文件元数据管理。
- 文件上传和下载。
- 文件夹管理。
- 搜索。
- 容量统计。
- 操作日志。
- 数据库访问。
- 为网页端、Windows 客户端和 Android 客户端提供统一接口。

## 3. 用户角色

### 3.1 普通用户

普通用户是系统的主要使用者，可以登录系统并管理自己的网盘文件。

主要权限：

- 注册、登录、退出登录。
- 查看个人网盘空间。
- 上传、下载、删除文件。
- 新建、重命名、移动、删除文件夹。
- 搜索文件。
- 查看个人存储空间使用情况。
- 通过网页端、Windows 客户端或 Android 客户端访问同一份文件数据。

### 3.2 管理员

管理员用于系统维护。

主要权限：

- 查看用户列表。
- 查看系统存储使用情况。
- 禁用或恢复用户账号。
- 查看系统运行日志。
- 管理用户容量。

## 4. 功能需求

## 4.1 用户模块

### 4.1.1 用户注册

用户可以通过用户名、邮箱和密码注册账号。

功能要求：

- 用户名不能为空，且不能重复。
- 邮箱格式必须合法，且不能重复。
- 密码需要加密存储，不能明文保存。
- 注册成功后可跳转到登录页或自动登录。
- 网页端、Windows 客户端和 Android 客户端注册后的账号数据一致。

### 4.1.2 用户登录

用户可以使用用户名或邮箱加密码登录系统。

功能要求：

- 登录成功后返回访问令牌或建立登录会话。
- 网页端可以保持登录状态。
- Windows 客户端和 Android 客户端可以保存登录凭证，避免频繁登录。
- 登录失败时提示账号或密码错误。
- 支持退出登录。
- 退出登录后本地登录凭证应失效或被清除。

### 4.1.3 用户信息

用户可以查看自己的基础信息。

展示内容：

- 用户名。
- 邮箱。
- 注册时间。
- 总容量。
- 已使用容量。
- 账号状态。

## 4.2 文件管理模块

### 4.2.1 文件上传

用户可以将本地文件上传到当前目录。

功能要求：

- 支持单文件上传。
- 支持多文件批量上传。
- 上传时显示进度。
- 上传成功后刷新文件列表。
- 文件名重复时可自动重命名，或提示用户覆盖/保留两份。
- 文件大小不能超过系统限制。
- 用户剩余容量不足时禁止上传。
- 网页端、Windows 客户端和 Android 客户端上传后看到的文件列表应保持一致。
- 上传失败时不能产生无效文件记录。
- 支持大文件上传。
- 支持上传任务失败重试。

### 4.2.2 文件下载

用户可以下载自己网盘中的文件。

功能要求：

- 单个文件可直接下载。
- 下载前需要校验用户是否拥有该文件权限。
- 网页端可使用浏览器默认下载能力。
- Windows 客户端应支持选择本地保存路径。
- Android 客户端应支持保存到设备可访问目录。
- 下载过程需要显示进度。
- 下载失败时需要提示用户，并支持重新下载。

### 4.2.3 文件列表

用户进入网盘首页或客户端文件页后，可以查看当前目录下的文件和文件夹。

列表字段：

- 文件名。
- 文件类型。
- 文件大小。
- 更新时间。
- 操作入口。

操作包括：

- 打开文件夹。
- 下载文件。
- 重命名。
- 移动。
- 删除。

### 4.2.4 新建文件夹

用户可以在当前目录中新建文件夹。

功能要求：

- 文件夹名称不能为空。
- 同一目录下文件夹名称不能重复。
- 文件夹名称不能包含非法字符。
- 新建后文件列表应立即刷新。

### 4.2.5 重命名

用户可以修改文件或文件夹名称。

功能要求：

- 新名称不能为空。
- 同一目录下不能与已有文件或文件夹重名。
- 文件重命名时需要保留或正确处理扩展名。
- 重命名后网页端、Windows 客户端和 Android 客户端展示一致。

### 4.2.6 移动文件

用户可以将文件或文件夹移动到其他目录。

功能要求：

- 支持选择目标文件夹。
- 不能将文件夹移动到自身或自己的子目录中。
- 移动后保持文件内容不变，仅修改目录关系。
- 移动后文件路径和列表展示应正确更新。

### 4.2.7 删除文件

用户可以删除文件或文件夹。

功能要求：

- 删除前需要二次确认。
- 删除文件夹时，其下所有文件和子文件夹一并删除。
- 删除后释放用户容量。
- 删除操作需要校验用户权限。
- 删除后的文件不提供系统内恢复能力。
- 数据库记录和文件数据需要保持一致，不能出现已删除文件仍可访问的情况。

## 4.3 搜索模块

用户可以根据关键词搜索自己的文件和文件夹。

功能要求：

- 支持按文件名模糊搜索。
- 支持按文件类型筛选。
- 支持按时间范围筛选。
- 搜索结果仅显示当前用户有权限访问的文件。
- 网页端、Windows 客户端和 Android 客户端搜索结果应保持一致。

## 4.4 容量管理模块

系统需要记录每个用户的空间使用情况。

功能要求：

- 用户拥有默认总容量。
- 管理员可以调整用户总容量。
- 上传文件前校验剩余空间。
- 删除文件后释放容量。
- 网页端、Windows 客户端和 Android 客户端中展示已使用容量和总容量。
- 容量统计需要与实际文件数据保持一致。

## 4.5 客户端任务模块

Windows 客户端和 Android 客户端需要管理上传和下载任务。

功能要求：

- 显示上传任务进度。
- 显示下载任务进度。
- 支持任务成功、失败、取消等状态展示。
- 支持失败任务重试。
- 网络异常时给出明确提示。
- 客户端重启后可查看最近任务记录。

## 4.6 管理员模块

管理员可以维护系统用户和存储状态。

功能要求：

- 查看用户列表。
- 查看用户详情。
- 禁用或恢复用户账号。
- 调整用户容量。
- 查看系统总存储使用情况。
- 查看关键操作日志。

## 5. 页面与客户端需求

## 5.1 Vue 3 网页端

### 5.1.1 登录页

页面内容：

- 用户名或邮箱输入框。
- 密码输入框。
- 登录按钮。
- 注册入口。
- 错误提示区域。

### 5.1.2 注册页

页面内容：

- 用户名输入框。
- 邮箱输入框。
- 密码输入框。
- 确认密码输入框。
- 注册按钮。
- 登录入口。

### 5.1.3 网盘首页

页面内容：

- 顶部导航栏。
- 当前路径面包屑。
- 上传按钮。
- 新建文件夹按钮。
- 搜索框。
- 文件列表。
- 容量使用信息。
- 用户菜单。

核心交互：

- 点击文件夹进入下一级目录。
- 点击文件可下载文件。
- 勾选文件后可批量删除或移动。
- 上传和下载时显示进度。

### 5.1.4 用户信息页

页面内容：

- 用户名。
- 邮箱。
- 总容量。
- 已使用容量。
- 注册时间。
- 账号状态。

### 5.1.5 管理员页面

页面内容：

- 用户列表。
- 用户容量信息。
- 用户账号状态。
- 系统存储统计。
- 操作日志列表。

## 5.2 Windows 客户端

### 5.2.1 登录窗口

窗口内容：

- 用户名或邮箱输入框。
- 密码输入框。
- 登录按钮。
- 记住登录状态选项。
- 错误提示区域。

### 5.2.2 主窗口

窗口内容：

- 远程目录树。
- 当前目录文件列表。
- 上传按钮。
- 下载按钮。
- 新建文件夹按钮。
- 删除按钮。
- 搜索框。
- 任务列表入口。
- 容量使用信息。

核心交互：

- 双击文件夹进入目录。
- 选择文件后下载到本地。
- 拖拽或选择本地文件上传。
- 右键菜单执行重命名、移动、删除等操作。
- 上传和下载任务显示实时进度。

### 5.2.3 任务窗口

窗口内容：

- 上传任务列表。
- 下载任务列表。
- 任务名称。
- 任务进度。
- 任务状态。
- 取消按钮。
- 重试按钮。

## 5.3 Android 客户端

### 5.3.1 登录页

页面内容：

- 用户名或邮箱输入框。
- 密码输入框。
- 登录按钮。
- 记住登录状态选项。
- 错误提示区域。

### 5.3.2 文件首页

页面内容：

- 当前路径。
- 文件和文件夹列表。
- 上传入口。
- 搜索入口。
- 容量使用信息。
- 用户菜单。

核心交互：

- 点击文件夹进入目录。
- 点击文件进行下载。
- 长按文件或文件夹弹出操作菜单。
- 支持上传手机本地文件。
- 上传和下载任务显示进度。

### 5.3.3 任务页

页面内容：

- 上传任务列表。
- 下载任务列表。
- 任务名称。
- 任务进度。
- 任务状态。
- 取消按钮。
- 重试按钮。

## 6. 数据需求

## 6.1 数据库要求

系统使用 MySQL 9.6 数据库保存用户、文件目录、文件元数据、文件内容、容量、任务和日志等数据。Java Spring Boot 后端通过 JDBC 接口访问数据库。

数据库访问要求：

- 所有数据库操作统一通过后端服务完成。
- 客户端和网页端不能直接连接数据库。
- JDBC 连接信息不能暴露给前端或客户端。
- 数据库连接需要使用连接池。
- 关键表需要建立索引。
- 涉及多表一致性的操作需要使用事务。
- 文件内容使用数据库 BLOB 字段或等价的大对象存储方式保存。
- 上传、下载大文件时后端需要使用流式读写，避免一次性将完整文件读入内存。

## 6.2 用户表

建议字段：

- id：用户 ID。
- username：用户名。
- email：邮箱。
- password_hash：密码哈希。
- total_capacity：总容量。
- used_capacity：已用容量。
- status：账号状态。
- created_at：创建时间。
- updated_at：更新时间。

## 6.3 文件表

建议字段：

- id：文件 ID。
- user_id：所属用户 ID。
- parent_id：父目录 ID。
- name：文件或文件夹名称。
- type：类型，file 或 folder。
- mime_type：文件 MIME 类型。
- size：文件大小。
- content_id：文件内容 ID，文件夹为空。
- md5：文件哈希。
- extension：扩展名。
- created_at：创建时间。
- updated_at：更新时间。

## 6.4 文件内容表

建议字段：

- id：文件内容 ID。
- file_id：文件 ID。
- content：文件二进制内容。
- size：文件大小。
- md5：文件哈希。
- created_at：创建时间。
- updated_at：更新时间。

## 6.5 操作日志表

建议字段：

- id：日志 ID。
- user_id：用户 ID。
- action：操作类型。
- target_id：操作对象 ID。
- target_type：操作对象类型。
- client_type：客户端类型，web、windows 或 android。
- ip_address：IP 地址。
- created_at：创建时间。

## 6.6 任务记录表

建议字段：

- id：任务 ID。
- user_id：用户 ID。
- file_id：文件 ID。
- task_type：任务类型，upload 或 download。
- client_type：客户端类型，web、windows 或 android。
- status：任务状态。
- progress：任务进度。
- error_message：错误信息。
- created_at：创建时间。
- updated_at：更新时间。

## 7. 非功能需求

## 7.1 安全性

- 密码必须加密存储。
- 登录接口需要防止暴力破解。
- 文件访问必须进行权限校验。
- 后端不能暴露真实文件存储路径。
- 上传文件需要限制大小。
- 接口需要防止越权访问。
- 网页端需要处理 XSS、CSRF 等常见安全问题。
- Windows 客户端和 Android 客户端需要安全保存登录凭证。
- JDBC 连接账号应遵循最小权限原则。

## 7.2 性能

- 文件列表分页或懒加载，避免大量文件一次性加载。
- 上传和下载过程应保持稳定。
- 常用查询字段需要建立索引。
- 大文件上传需要控制内存占用，避免一次性读入完整文件。
- 后端接口需要同时满足网页端、Windows 客户端和 Android 客户端访问。
- 数据库访问需要使用连接池提升并发能力。

## 7.3 可靠性

- 文件元数据和文件内容需要保持一致。
- 上传失败时不能产生脏数据。
- 删除文件时需要处理数据库和文件数据之间的一致性。
- 关键操作应记录日志。
- 网络异常时三个客户端都应给出明确提示。
- 数据库操作失败时应回滚相关事务。

## 7.4 易用性

- 网页端和客户端操作需要清晰直观。
- 上传、下载、删除、移动等操作需要有明确反馈。
- 文件大小、时间等信息需要以易读格式展示。
- 出错时给出友好的提示信息。
- Android 客户端需要适配触控操作和小屏幕布局。

## 7.5 兼容性

- 网页端支持主流现代浏览器。
- Windows 客户端支持 Windows 10 及以上系统。
- Android 客户端支持 Android 8.0 及以上系统。

## 8. 技术要求

### 8.1 网页端

- Vue 3。
- TypeScript。
- Vite。
- Axios 或 Fetch。
- Element Plus 或其他 Vue 3 组件库。

### 8.2 Windows 客户端

Windows 客户端技术方案可选择：

- Electron + Vue 3。
- JavaFX。
- WPF。

如希望复用网页端界面和开发经验，推荐使用 Electron + Vue 3。

### 8.3 Android 客户端

Android 客户端技术方案可选择：

- Kotlin + Android 原生开发。
- Java + Android 原生开发。

推荐使用 Kotlin 或 Java 原生开发，通过 HTTP API 调用 Spring Boot 后端。

### 8.4 后端

- Java。
- Spring Boot。
- Spring MVC 或 WebFlux。
- JDBC。
- MySQL 9.6 JDBC 驱动。
- 数据库连接池，例如 HikariCP。

后端需要对外提供统一 REST API，网页端、Windows 客户端和 Android 客户端都通过该 API 访问文件和用户数据。

### 8.5 数据库与存储

- 数据库使用 MySQL 9.6。
- 后端通过 JDBC 接口访问 MySQL。
- 用户、文件元数据、文件内容、目录关系、容量数据、任务记录、操作日志均保存在 MySQL 中。
- 文件内容使用 MySQL BLOB 或等价大对象字段保存。
- 后端上传和下载文件时必须使用 JDBC 流式读写。
- 需要重点关注数据库容量、备份恢复和大文件读写性能。

## 9. 接口需求示例

## 9.1 用户接口

- POST /api/auth/register：用户注册。
- POST /api/auth/login：用户登录。
- POST /api/auth/logout：退出登录。
- GET /api/user/profile：获取用户信息。

## 9.2 文件接口

- GET /api/files：获取文件列表。
- POST /api/files/upload：上传文件。
- GET /api/files/{id}/download：下载文件。
- POST /api/folders：新建文件夹。
- PATCH /api/files/{id}/rename：重命名文件。
- PATCH /api/files/{id}/move：移动文件。
- DELETE /api/files/{id}：删除文件。

## 9.3 搜索接口

- GET /api/search/files：搜索文件和文件夹。

## 9.4 任务接口

- GET /api/tasks：获取任务列表。
- GET /api/tasks/{id}：获取任务详情。
- POST /api/tasks/{id}/cancel：取消任务。
- POST /api/tasks/{id}/retry：重试任务。

## 9.5 管理员接口

- GET /api/admin/users：获取用户列表。
- GET /api/admin/users/{id}：获取用户详情。
- PATCH /api/admin/users/{id}/status：修改用户状态。
- PATCH /api/admin/users/{id}/capacity：修改用户容量。
- GET /api/admin/storage：获取系统存储统计。
- GET /api/admin/logs：获取操作日志。

## 10. 最终交付范围

本项目不再拆分 MVP 或版本规划，直接以最终版本为开发目标。

最终需要完成：

- Java + Spring Boot 统一后端。
- JDBC 方式访问 MySQL 9.6 数据库。
- Vue 3 网页端。
- Windows 客户端。
- Android 客户端。
- 用户注册、登录、退出。
- 管理员用户管理和容量管理。
- 文件上传、下载、删除、重命名、移动。
- 文件夹新建、进入、重命名、移动、删除。
- 文件搜索。
- 用户容量统计。
- 上传和下载任务进度展示。
- 操作日志记录。
- 网页端、Windows 客户端、Android 客户端数据一致。

不包含以下功能：

- 文件在线预览。
- 文件分享。
- 回收站。
- 文件系统内恢复。
- macOS 客户端。
- Linux 客户端。

## 11. 验收标准

### 11.1 用户模块

- 用户可以成功注册账号。
- 用户可以使用正确账号密码登录。
- 用户输入错误密码时无法登录。
- 登录后可以正常退出。
- 网页端、Windows 客户端和 Android 客户端都可以使用同一账号登录。

### 11.2 文件模块

- 用户可以通过网页端上传文件，并在 Windows 和 Android 客户端看到该文件。
- 用户可以通过 Windows 客户端上传文件，并在网页端和 Android 客户端看到该文件。
- 用户可以通过 Android 客户端上传文件，并在网页端和 Windows 客户端看到该文件。
- 用户可以下载自己上传的文件。
- 用户不能访问其他用户的文件。
- 用户可以新建文件夹。
- 用户可以进入文件夹并返回上级目录。
- 用户可以重命名文件或文件夹。
- 用户可以移动文件或文件夹。
- 用户可以删除文件。
- 删除文件后用户容量正确变化。

### 11.3 搜索模块

- 用户可以通过关键词搜索自己的文件和文件夹。
- 搜索结果不会包含其他用户的文件。
- 网页端、Windows 客户端和 Android 客户端搜索结果一致。

### 11.4 客户端模块

- Windows 客户端可以连接后端服务。
- Android 客户端可以连接后端服务。
- 客户端登录后可以获取远程文件列表。
- 客户端上传和下载时可以显示任务进度。
- 网络异常时客户端可以提示用户。

### 11.5 管理员模块

- 管理员可以查看用户列表。
- 管理员可以禁用和恢复用户账号。
- 管理员可以调整用户容量。
- 管理员可以查看系统存储统计。
- 管理员可以查看操作日志。

## 12. 风险与注意事项

- 文件权限校验是核心风险，所有文件接口都必须校验用户身份和文件归属。
- 网页端、Windows 客户端和 Android 客户端共用后端接口，需要保持接口设计稳定。
- 文件删除和数据删除需要保持一致，避免产生垃圾数据或误删文件。
- Windows 客户端和 Android 客户端保存登录凭证时需要注意安全。
- 大文件上传对网络稳定性、后端资源占用和数据库压力影响较大。
- 文件内容直接存入 MySQL，需要重点评估大文件性能、备份体积和数据库恢复成本。
- JDBC 数据库账号、密码和连接地址不能暴露给前端或客户端。
