# 详细设计-Windows客户端设计

## 1. 技术栈

- Electron。
- Vue 3。
- TypeScript。
- Vite。
- Axios。
- Pinia。

Windows 客户端复用网页端的部分 Vue 组件，但需要通过 Electron 主进程访问本地文件选择、目录选择和安全存储能力。

## 2. 进程设计

### 2.1 主进程

职责：

- 创建登录窗口和主窗口。
- 管理应用生命周期。
- 提供文件选择对话框。
- 提供下载目录选择对话框。
- 提供安全凭证存储接口。

### 2.2 渲染进程

职责：

- 展示登录界面、文件界面和任务界面。
- 调用后端 REST API。
- 展示上传下载进度。
- 通过 IPC 调用主进程能力。

### 2.3 preload

暴露安全 API：

```ts
window.mycloud = {
  selectFiles(): Promise<LocalFile[]>
  selectDirectory(): Promise<string | null>
  saveToken(token: string): Promise<void>
  getToken(): Promise<string | null>
  clearToken(): Promise<void>
}
```

## 3. 窗口设计

### 3.1 登录窗口

字段：

- 账号。
- 密码。
- 记住登录状态。

逻辑：

- 登录成功保存 token。
- 跳转主窗口。
- 登录失败显示错误提示。

### 3.2 主窗口

区域：

- 顶部工具栏。
- 远程目录树。
- 当前目录文件列表。
- 容量信息。
- 任务入口。

操作：

- 双击文件夹进入目录。
- 选择文件下载。
- 拖拽或选择本地文件上传。
- 右键菜单执行重命名、移动、删除。

### 3.3 任务窗口

展示：

- 上传任务。
- 下载任务。
- 任务状态。
- 进度条。
- 取消按钮。
- 重试按钮。

## 4. 本地存储设计

token 保存方式：

- 优先使用系统安全凭证存储。
- 不保存明文密码。
- 退出登录时清理 token。

客户端配置：

```json
{
  "serverBaseUrl": "https://example.com",
  "defaultDownloadDir": "D:\\Downloads"
}
```

## 5. API 调用设计

所有请求附加：

```text
Authorization: Bearer <jwt>
X-Client-Type: windows
```

请求封装统一处理：

- 登录过期。
- 网络异常。
- 服务端错误。
- 下载流。
- 上传进度。

## 6. 文件上传设计

上传来源：

- 选择本地文件。
- 拖拽文件到文件列表区域。

流程：

1. 通过主进程选择文件。
2. 渲染进程创建上传任务。
3. 使用 multipart 调用上传接口。
4. 根据上传进度更新任务窗口。
5. 成功后刷新当前目录。

## 7. 文件下载设计

流程：

1. 用户选择远程文件。
2. 选择本地保存目录。
3. 调用下载接口。
4. 将响应流写入本地文件。
5. 展示进度。
6. 下载完成后更新任务状态。

文件名冲突策略：

- 如果本地已有同名文件，默认自动追加 `(1)`。

## 8. 异常处理

| 场景 | 处理 |
| --- | --- |
| 后端不可达 | 显示网络异常，可重试 |
| token 过期 | 清理 token，返回登录窗口 |
| 上传失败 | 任务标记 failed，允许重试 |
| 下载失败 | 删除未完成临时文件，允许重试 |
| 权限不足 | 提示并刷新当前目录 |

## 9. 与网页端复用

可复用：

- API 封装模型。
- 文件列表组件逻辑。
- 任务状态枚举。
- 文件大小和时间格式化工具。

不可直接复用：

- 浏览器下载逻辑。
- 浏览器 localStorage token 策略。
- DOM 文件选择行为。
